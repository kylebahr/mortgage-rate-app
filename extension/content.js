// Mortgage Rate Tracker - Parent Page Content Script
// Runs on Commerce Bank mortgage rates page
// The actual form filling happens in iframe-content.js which runs inside the OptimalBlue iframe

console.log('[MortgageTracker] Parent page content script loaded');
console.log('[MortgageTracker] Waiting for OptimalBlue iframe to load and process...');

// This script doesn't need to do anything - the iframe-content.js handles everything
// We just log that we're on the right page
